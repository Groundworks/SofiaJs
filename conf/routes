# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /oauth2                     controllers.Authenticator.oauth2Callback
POST    /oauth2cred                 controllers.Authenticator.oauth2
POST    /cred                       controllers.Authenticator.oauth2

POST    /auth                       controllers.Application.auth
POST    /content                    controllers.Application.content
POST    /update                     controllers.Application.update
OPTIONS /content                    controllers.Application.options
POST    /push                       controllers.Application.push
POST    /site                       controllers.Application.site
POST    /pullrequest                controllers.Application.pullrequest
POST    /pullrequests               controllers.Application.pullrequests

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
GET     /current.js                 controllers.Application.current
GET     /*default                   controllers.Application.page(default)
